import React from 'react';
import './index.css';

const NotAdmin = props => (
  <div className="NotAdmin">
    <h1 className="NotAdmin-header">You're not an admin!</h1>
  </div>
)

export default NotAdmin;
